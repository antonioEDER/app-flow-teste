
{
    "apps":
[
    {
        "appId": "9da80750",
        "root": "src-capacitor",
        "dependencyInstallCommand": "cd .. && yarn install && yarn android && src-capacitor",
        "webBuildCommand": "npx quasar build -m capacitor -T android --skip-pkg"
    }
]
}